@page "/settings"
@inject NavigationManager Navigation

<SettingsModal IsOpen="true" OnClose="CloseAsync" />

@code {
    private Task CloseAsync()
    {
        Navigation.NavigateTo("/");
        return Task.CompletedTask;
    }
}
