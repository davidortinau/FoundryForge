# OS
.DS_Store
Thumbs.db

# Node (Squad CLI artifacts; project itself is .NET)
node_modules/

# .NET
bin/
obj/
*.user
*.suo

# Squad — keep team state, ignore noisy runtime logs
.squad/log/
.squad/orchestration-log/
.squad/casting-history.json

# spec-kit — keep artifacts, ignore caches
.specify/cache/

# Local
.env
.env.local
*.secrets
# Squad: ignore runtime state (logs, inbox, sessions)
.squad/decisions/inbox/
.squad/sessions/
.squad/.scratch/
.squad/.cache/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream

# M0 feasibility spikes — throwaway proof heads, not tracked
spikes/
